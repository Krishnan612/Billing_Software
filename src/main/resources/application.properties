spring.datasource.url=jdbc:mysql://localhost:3306/billing_app
spring.datasource.username=${YOUR_USERNAME}
spring.datasource.password=${YOUR_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
server.servlet.context-path=/api/v1.0
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

aws.access.key=${YOUR_KEY}
aws.secret.key=${YOUR_KEY}
aws.region=${YOUR_REGION}
aws.bucket.name=${YOUR_BUCKETNAME}

jwt.secret.key=thisismysecretkeyfortheupcomingproject

razorpay.key.id=${YOUR_RAZORPAY_ID}
razorpay.key.secret=${YOUR_RAZORPAU_SECRET}
